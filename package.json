{
  "name": "javascript-excel-task",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start-dev": "nodemon src/index.js",
    "vitest-dev": "vitest watch --dir tests",
    "vitest-dev-verbose": "vitest watch --dir tests --reporter verbose",
    "vitest-dev-verbose-coverage": "vitest watch --dir tests --reporter verbose --coverage",
    "vitest-dev-ui": "vitest watch --dir tests --reporter verbose --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-c8": "^0.29.8",
    "@vitest/ui": "^0.29.8",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "vitest": "^0.29.8"
  }
}
